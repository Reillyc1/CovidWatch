# CovidWatch Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit your actual .env file to version control

# ===========================================
# Database Configuration
# ===========================================
# The hostname where your MySQL database is running
DB_HOST=localhost

# MySQL username (avoid using root in production)
DB_USER=root

# MySQL password (use a strong password in production)
DB_PASSWORD=your_password_here

# Name of the database to use
DB_NAME=covidwatch

# ===========================================
# Session Configuration (REQUIRED)
# ===========================================
# Secret key used to sign session cookies
# IMPORTANT: Generate a strong random string for production:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=your-session-secret-generate-a-64-byte-hex-string

# ===========================================
# Application Settings
# ===========================================
# Set to 'production' when deploying to a live server
# This enables security features like:
# - Secure cookies (HTTPS only)
# - Strict session validation
# - Error message hiding
NODE_ENV=development

# Port the server will listen on (default: 3000)
PORT=3000

# ===========================================
# Mapbox Configuration (REQUIRED for map feature)
# ===========================================
# Get your token at https://mapbox.com
# Use a public token and restrict it by URL in the Mapbox dashboard
MAPBOX_TOKEN=your_mapbox_public_token_here

# ===========================================
# Security Configuration
# ===========================================
# Trust proxy headers (set to 1 if behind a reverse proxy like nginx)
# This is important for accurate rate limiting
# TRUST_PROXY=1
